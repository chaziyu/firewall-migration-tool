domain: system
part: 5
parts: 8
sections:
- path: system lte-modem
  mode: singleton
  fields:
    allow-modify-mtu-size: enum
    allow-modify-wireless-profile-table: enum
    apn: string
    authtype: enum
    auto-connect: enum
    band-restrictions: string
    data-usage-tracking: enum
    dhcp-relay: enum
    extra-init: string
    force-wireless-profile: integer
    gps-port: integer
    gps-service: enum
    holddown-timer: integer
    image-preference: enum
    interface: string
    manual-handover: enum
    mode: enum
    modem-port: integer
    network-type: enum
    override-gateway: enum
    passwd: password!sensitive
    pdptype: option
    sim1-pin: password!sensitive
    sim2-pin: password!sensitive
    status: enum
    username: string
  nested:
  - name: data-plan
    mode: table
    key: <name>
    fields:
      billing-date: integer
      billing-hour: integer
      billing-period: enum
      billing-weekday: enum
      data-limit: integer
      data-limit-alert: integer
      delay-switch-time: string
      iccid: string
      overage: enum
      target-sim-slot: enum
  - name: sim-switch
    mode: singleton
    fields:
      by-connection-state: enum
      by-data-plan: enum
      by-link-monitor: enum
      by-sim-state: enum
      link-monitor: string
      modem-disconnection-time: integer
      sim-slot: integer
      sim-switch-log-alert-interval: integer
      sim-switch-log-alert-threshold: integer
  model_dependent: true
- path: system mac-address-table
  mode: table
  key: <mac>
  fields:
    interface: string
    reply-substitute: mac-address
- path: system management-tunnel
  mode: singleton
  fields:
    allow-collect-statistics: enum
    allow-config-restore: enum
    allow-push-configuration: enum
    allow-push-firmware: enum
    authorized-manager-only: enum
    serial-number: opaque-user
    status: enum
- path: system mobile-tunnel
  mode: table
  key: <name>
  fields:
    hash-algorithm: option
    home-address: ipv4-address
    home-agent: ipv4-address
    lifetime: integer
    n-mhae-key: password_aes256!sensitive
    n-mhae-key-type: enum
    n-mhae-spi: integer
    reg-interval: integer
    reg-retry: integer
    renew-interval: integer
    roaming-interface: string
    status: enum
    tunnel-mode: option
  nested:
  - name: network
    mode: table
    key: <id>
    fields:
      interface: string
      prefix: ipv4-classnet
- path: system modem
  mode: singleton
  fields:
    action: enum
    altmode: enum
    authtype1: option-list
    authtype2: option-list
    authtype3: option-list
    auto-dial: enum
    connect-timeout: integer
    dial-cmd1: string
    dial-cmd2: string
    dial-cmd3: string
    dial-on-demand: enum
    distance: integer
    dont-send-CR1: enum
    dont-send-CR2: enum
    dont-send-CR3: enum
    extra-init1: string
    extra-init2: string
    extra-init3: string
    holddown-timer: integer
    idle-timer: integer
    interface: string
    lockdown-lac: string
    mode: enum
    network-init: string
    passwd1: password!sensitive
    passwd2: password!sensitive
    passwd3: password!sensitive
    peer-modem1: enum
    peer-modem2: enum
    peer-modem3: enum
    phone1: string
    phone2: string
    phone3: string
    pin-init: string
    ppp-echo-request1: enum
    ppp-echo-request2: enum
    ppp-echo-request3: enum
    priority: integer
    redial: enum
    reset: integer
    status: enum
    traffic-check: enum
    username1: string
    username2: string
    username3: string
    wireless-port: integer
  model_dependent: true
- path: system nd-proxy
  mode: singleton
  fields:
    member: identifier-list
    status: enum
- path: system netflow
  mode: singleton
  fields:
    active-flow-timeout: integer
    inactive-flow-timeout: integer
    template-tx-counter: integer
    template-tx-timeout: integer
  nested:
  - name: collectors
    mode: table
    key: <id>
    fields:
      collector-ip: string
      collector-port: integer
      interface: string
      interface-select-method: enum
      source-ip: string
- path: system network-visibility
  mode: singleton
  fields:
    destination-hostname-visibility: enum
    destination-location: enum
    destination-visibility: enum
    hostname-limit: integer
    hostname-ttl: integer
    source-location: enum
- path: system np6
  mode: table
  key: <name>
  fields:
    fastpath: enum
    garbage-session-collector: enum
    ipsec-ob-hash-function: enum
    ipsec-outbound-hash: enum
    low-latency-mode: enum
    per-session-accounting: enum
    session-collector-interval: integer
    session-timeout-fixed: enum
    session-timeout-interval: integer
    session-timeout-random-range: integer
  nested:
  - name: fp-anomaly
    mode: singleton
    fields:
      icmp-csum-err: enum
      icmp-frag: enum
      icmp-land: enum
      ipv4-csum-err: enum
      ipv4-land: enum
      ipv4-optlsrr: enum
      ipv4-optrr: enum
      ipv4-optsecurity: enum
      ipv4-optssrr: enum
      ipv4-optstream: enum
      ipv4-opttimestamp: enum
      ipv4-proto-err: enum
      ipv4-unknopt: enum
      ipv6-daddr-err: enum
      ipv6-land: enum
      ipv6-optendpid: enum
      ipv6-opthomeaddr: enum
      ipv6-optinvld: enum
      ipv6-optjumbo: enum
      ipv6-optnsap: enum
      ipv6-optralert: enum
      ipv6-opttunnel: enum
      ipv6-proto-err: enum
      ipv6-saddr-err: enum
      ipv6-unknopt: enum
      tcp-csum-err: enum
      tcp-fin-noack: enum
      tcp-fin-only: enum
      tcp-land: enum
      tcp-no-flag: enum
      tcp-syn-data: enum
      tcp-syn-fin: enum
      tcp-winnuke: enum
      udp-csum-err: enum
      udp-land: enum
  - name: hpe
    mode: singleton
    fields:
      arp-max: integer
      enable-shaper: enum
      esp-max: integer
      icmp-max: integer
      ip-frag-max: integer
      ip-others-max: integer
      l2-others-max: integer
      pri-type-max: integer
      sctp-max: integer
      tcp-max: integer
      tcpfin-rst-max: integer
      tcpsyn-ack-max: integer
      tcpsyn-max: integer
      udp-max: integer
  model_dependent: true
- path: system np6xlite
  mode: table
  key: <name>
  fields:
    congestion-handling-mode: enum
    fastpath: enum
    garbage-session-collector: enum
    ipsec-inner-fragment: enum
    ipsec-sts-timeout: enum
    ipsec-throughput-msg-frequency: enum
    per-session-accounting: enum
    session-collector-interval: integer
    session-timeout-fixed: enum
    session-timeout-interval: integer
    session-timeout-random-range: integer
  nested:
  - name: fp-anomaly
    mode: singleton
    fields:
      icmp-csum-err: enum
      icmp-frag: enum
      icmp-land: enum
      ipv4-csum-err: enum
      ipv4-land: enum
      ipv4-optlsrr: enum
      ipv4-optrr: enum
      ipv4-optsecurity: enum
      ipv4-optssrr: enum
      ipv4-optstream: enum
      ipv4-opttimestamp: enum
      ipv4-proto-err: enum
      ipv4-unknopt: enum
      ipv6-daddr-err: enum
      ipv6-land: enum
      ipv6-optendpid: enum
      ipv6-opthomeaddr: enum
      ipv6-optinvld: enum
      ipv6-optjumbo: enum
      ipv6-optnsap: enum
      ipv6-optralert: enum
      ipv6-opttunnel: enum
      ipv6-proto-err: enum
      ipv6-saddr-err: enum
      ipv6-unknopt: enum
      tcp-csum-err: enum
      tcp-fin-noack: enum
      tcp-fin-only: enum
      tcp-land: enum
      tcp-no-flag: enum
      tcp-syn-data: enum
      tcp-syn-fin: enum
      tcp-winnuke: enum
      udp-csum-err: enum
      udp-land: enum
  - name: hpe
    mode: singleton
    fields:
      arp-max: integer
      enable-shaper: enum
      esp-max: integer
      icmp-max: integer
      ip-frag-max: integer
      ip-others-max: integer
      l2-others-max: integer
      pri-type-max: integer
      sctp-max: integer
      tcp-others-max: integer
      tcpfin-rst-max: integer
      tcpsyn-ack-max: integer
      tcpsyn-max: integer
      udp-max: integer
  model_dependent: true
- path: system npu-post
  mode: singleton
  fields:
    npu-group-effective-scope: integer
  nested:
  - name: port-npu-map
    mode: table
    key: <interface>
    fields:
      npu-group: identifier-list
  model_dependent: true
- path: system npu-setting prp
  mode: singleton
  fields:
    prp-port-in: identifier-list
    prp-port-out: identifier-list
  model_dependent: true
- path: system npu-vlink
  mode: table
  key: <name>
  model_dependent: true
