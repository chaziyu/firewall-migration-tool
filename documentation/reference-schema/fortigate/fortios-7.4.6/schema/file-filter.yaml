domain: file-filter
part: 1
parts: 1
sections:
- path: file-filter profile
  mode: table
  key: <name>
  fields:
    comment: var-string
    extended-log: enum
    feature-set: enum
    log: enum
    replacemsg-group: string
    scan-archive-contents: enum
  nested:
  - name: rules
    mode: table
    key: <name>
    fields:
      action: enum
      comment: var-string
      direction: enum
      file-type: identifier-list
      password-protected: enum
      protocol: option-list
