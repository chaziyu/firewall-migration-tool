domain: voip
part: 1
parts: 1
sections:
- path: voip profile
  mode: table
  key: <name>
  fields:
    comment: var-string
    feature-set: enum
  nested:
  - name: msrp
    mode: singleton
    fields:
      log-violations: enum
      max-msg-size: integer
      max-msg-size-action: enum
      status: enum
  - name: sccp
    mode: singleton
    fields:
      block-mcast: enum
      log-call-summary: enum
      log-violations: enum
      max-calls: integer
      status: enum
      verify-header: enum
  - name: sip
    mode: singleton
    fields:
      ack-rate: integer
      ack-rate-track: enum
      block-ack: enum
      block-bye: enum
      block-cancel: enum
      block-geo-red-options: enum
      block-info: enum
      block-invite: enum
      block-long-lines: enum
      block-message: enum
      block-notify: enum
      block-options: enum
      block-prack: enum
      block-publish: enum
      block-refer: enum
      block-register: enum
      block-subscribe: enum
      block-unknown: enum
      block-update: enum
      bye-rate: integer
      bye-rate-track: enum
      call-id-regex: var-string
      call-keepalive: integer
      cancel-rate: integer
      cancel-rate-track: enum
      contact-fixup: enum
      content-type-regex: var-string
      hnt-restrict-source-ip: enum
      hosted-nat-traversal: enum
      info-rate: integer
      info-rate-track: enum
      invite-rate: integer
      invite-rate-track: enum
      ips-rtp: enum
      log-call-summary: enum
      log-violations: enum
      malformed-header-allow: enum
      malformed-header-call-id: enum
      malformed-header-contact: enum
      malformed-header-content-length: enum
      malformed-header-content-type: enum
      malformed-header-cseq: enum
      malformed-header-expires: enum
      malformed-header-from: enum
      malformed-header-max-forwards: enum
      malformed-header-no-proxy-require: enum
      malformed-header-no-require: enum
      malformed-header-p-asserted-identity: enum
      malformed-header-rack: enum
      malformed-header-record-route: enum
      malformed-header-route: enum
      malformed-header-rseq: enum
      malformed-header-sdp-a: enum
      malformed-header-sdp-b: enum
      malformed-header-sdp-c: enum
      malformed-header-sdp-i: enum
      malformed-header-sdp-k: enum
      malformed-header-sdp-m: enum
      malformed-header-sdp-o: enum
      malformed-header-sdp-r: enum
      malformed-header-sdp-s: enum
      malformed-header-sdp-t: enum
      malformed-header-sdp-v: enum
      malformed-header-sdp-z: enum
      malformed-header-to: enum
      malformed-header-via: enum
      malformed-request-line: enum
      max-body-length: integer
      max-dialogs: integer
      max-idle-dialogs: integer
      max-line-length: integer
      message-rate: integer
      message-rate-track: enum
      nat-port-range: opaque-user
      nat-trace: enum
      no-sdp-fixup: enum
      notify-rate: integer
      notify-rate-track: enum
      open-contact-pinhole: enum
      open-record-route-pinhole: enum
      open-register-pinhole: enum
      open-via-pinhole: enum
      options-rate: integer
      options-rate-track: enum
      prack-rate: integer
      prack-rate-track: enum
      preserve-override: enum
      provisional-invite-expiry-time: integer
      publish-rate: integer
      publish-rate-track: enum
      refer-rate: integer
      refer-rate-track: enum
      register-contact-trace: enum
      register-rate: integer
      register-rate-track: enum
      rfc2543-branch: enum
      rtp: enum
      ssl-algorithm: enum
      ssl-auth-client: string
      ssl-auth-server: string
      ssl-client-certificate: string
      ssl-client-renegotiation: enum
      ssl-max-version: enum
      ssl-min-version: enum
      ssl-mode: enum
      ssl-pfs: enum
      ssl-send-empty-frags: enum
      ssl-server-certificate: string
      status: enum
      strict-register: enum
      subscribe-rate: integer
      subscribe-rate-track: enum
      unknown-header: enum
      update-rate: integer
      update-rate-track: enum
