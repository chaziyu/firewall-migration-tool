domain: ftp-proxy
part: 1
parts: 1
sections:
- path: ftp-proxy explicit
  mode: singleton
  fields:
    incoming-ip: ipv4-address-any
    incoming-port: opaque-user
    outgoing-ip: ipv4-address-any
    sec-default-action: enum
    server-data-mode: enum
    ssl: enum
    ssl-algorithm: enum
    ssl-cert: identifier-list
    ssl-dh-bits: enum
    status: enum
