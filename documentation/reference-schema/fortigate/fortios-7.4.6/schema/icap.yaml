domain: icap
part: 1
parts: 1
sections:
- path: icap profile
  mode: table
  key: <name>
  fields:
    204-response: enum
    204-size-limit: integer
    chunk-encap: enum
    comment: var-string
    extension-feature: option-list
    file-transfer: option-list
    file-transfer-failure: enum
    file-transfer-path: string
    file-transfer-server: string
    icap-block-log: enum
    methods: option-list
    preview: enum
    preview-data-length: integer
    replacemsg-group: string
    request: enum
    request-failure: enum
    request-path: string
    request-server: string
    respmod-default-action: enum
    response: enum
    response-failure: enum
    response-path: string
    response-req-hdr: enum
    response-server: string
    scan-progress-interval: integer
    streaming-content-bypass: enum
    timeout: integer
  nested:
  - name: icap-headers
    mode: table
    key: <id>
    fields:
      base64-encoding: enum
      content: string
      name: string
  - name: respmod-forward-rules
    mode: table
    key: <name>
    fields:
      action: enum
      host: string
      http-resp-status-code: identifier-list
    nested:
    - name: header-group
      mode: table
      key: <id>
      fields:
        case-sensitivity: enum
        header: string
        header-name: string
- path: icap server-group
  mode: table
  key: <name>
  fields:
    ldb-method: enum
  nested:
  - name: server-list
    mode: table
    key: <name>
    fields:
      weight: integer
- path: icap server
  mode: table
  key: <name>
  fields:
    addr-type: enum
    fqdn: string
    healthcheck: enum
    healthcheck-service: string
    ip-address: ipv4-address-any
    ip6-address: ipv6-address
    max-connections: integer
    port: integer
    secure: enum
    ssl-cert: string
