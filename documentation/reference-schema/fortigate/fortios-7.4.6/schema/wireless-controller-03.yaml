domain: wireless-controller
part: 3
parts: 3
sections:
- path: wireless-controller wtp-profile
  mode: table
  key: <name>
  fields:
    allowaccess: option-list
    ap-country: enum
    ap-handoff: enum
    apcfg-profile: string
    ble-profile: string
    bonjour-profile: string
    comment: var-string
    console-login: enum
    control-message-offload: option-list
    dtls-in-kernel: enum
    dtls-policy: option-list
    energy-efficient-ethernet: enum
    ext-info-enable: enum
    frequency-handoff: enum
    handoff-roaming: enum
    handoff-rssi: integer
    handoff-sta-thresh: integer
    indoor-outdoor-deployment: enum
    ip-fragment-preventing: option-list
    led-schedules: identifier-list
    led-state: enum
    lldp: enum
    login-passwd: password!sensitive
    login-passwd-change: enum
    max-clients: integer
    poe-mode: enum
    split-tunneling-acl-local-ap-subnet: enum
    split-tunneling-acl-path: enum
    syslog-profile: string
    tun-mtu-downlink: integer
    tun-mtu-uplink: integer
    unii-4-5ghz-band: enum
    usb-port: enum
    wan-port-auth: enum
    wan-port-auth-macsec: enum
    wan-port-auth-methods: enum
    wan-port-auth-password: password!sensitive
    wan-port-auth-usrname: string
    wan-port-mode: enum
  nested:
  - name: deny-mac-list
    mode: table
    key: <id>
    fields:
      mac: mac-address
  - name: esl-ses-dongle
    mode: singleton
    fields:
      apc-addr-type: enum
      apc-fqdn: string
      apc-ip: ipv4-address
      apc-port: integer
      coex-level: option
      compliance-level: option
      esl-channel: enum
      output-power: enum
      scd-enable: enum
      tls-cert-verification: enum
      tls-fqdn-verification: enum
  - name: lan
    mode: singleton
    fields:
      port-esl-mode: enum
      port-esl-ssid: string
      port-mode: enum
      port-ssid: string
      port1-mode: enum
      port1-ssid: string
      port2-mode: enum
      port2-ssid: string
      port3-mode: enum
      port3-ssid: string
      port4-mode: enum
      port4-ssid: string
      port5-mode: enum
      port5-ssid: string
      port6-mode: enum
      port6-ssid: string
      port7-mode: enum
      port7-ssid: string
      port8-mode: enum
      port8-ssid: string
  - name: lbs
    mode: singleton
    fields:
      aeroscout: enum
      aeroscout-ap-mac: enum
      aeroscout-mmu-report: enum
      aeroscout-mu: enum
      aeroscout-mu-factor: integer
      aeroscout-mu-timeout: integer
      aeroscout-server-ip: ipv4-address-any
      aeroscout-server-port: integer
      ble-rtls: enum
      ble-rtls-accumulation-interval: integer
      ble-rtls-asset-addrgrp-list: string
      ble-rtls-asset-uuid-list1: string
      ble-rtls-asset-uuid-list2: string
      ble-rtls-asset-uuid-list3: string
      ble-rtls-asset-uuid-list4: string
      ble-rtls-protocol: option
      ble-rtls-reporting-interval: integer
      ble-rtls-server-fqdn: string
      ble-rtls-server-path: string
      ble-rtls-server-port: integer
      ble-rtls-server-token: string
      ekahau-blink-mode: enum
      ekahau-tag: mac-address
      erc-server-ip: ipv4-address-any
      erc-server-port: integer
      fortipresence: enum
      fortipresence-ble: enum
      fortipresence-frequency: integer
      fortipresence-port: integer
      fortipresence-project: string
      fortipresence-rogue: enum
      fortipresence-secret: password!sensitive
      fortipresence-server: ipv4-address-any
      fortipresence-server-addr-type: enum
      fortipresence-server-fqdn: string
      fortipresence-unassoc: enum
      station-locate: enum
  - name: platform
    mode: singleton
    fields:
      ddscan: enum
      mode: enum
      type: enum
  - name: radio-1
    mode: singleton
    fields:
      80211d: enum
      airtime-fairness: enum
      amsdu: enum
      ap-sniffer-addr: mac-address
      ap-sniffer-bufsize: integer
      ap-sniffer-chan: integer
      ap-sniffer-chan-width: enum
      ap-sniffer-ctl: enum
      ap-sniffer-data: enum
      ap-sniffer-mgmt-beacon: enum
      ap-sniffer-mgmt-other: enum
      ap-sniffer-mgmt-probe: enum
      arrp-profile: string
      auto-power-high: integer
      auto-power-level: enum
      auto-power-low: integer
      auto-power-target: string
      band: option-list
      band-5g-type: enum
      bandwidth-admission-control: enum
      bandwidth-capacity: integer
      beacon-interval: integer
      bss-color: integer
      bss-color-mode: enum
      call-admission-control: enum
      call-capacity: integer
      channel: identifier-list
      channel-bonding: enum
      channel-bonding-ext: enum
      channel-utilization: enum
      coexistence: enum
      darrp: enum
      drma: enum
      drma-sensitivity: enum
      dtim: integer
      frag-threshold: integer
      iperf-protocol: enum
      iperf-server-port: integer
      max-clients: integer
      max-distance: integer
      mimo-mode: enum
      mode: enum
      optional-antenna: enum
      optional-antenna-gain: string
      power-level: integer
      power-mode: enum
      power-value: integer
      powersave-optimize: option-list
      protection-mode: enum
      rts-threshold: integer
      sam-bssid: mac-address
      sam-ca-certificate: string
      sam-captive-portal: enum
      sam-client-certificate: string
      sam-cwp-failure-string: string
      sam-cwp-match-string: string
      sam-cwp-password: password!sensitive
      sam-cwp-success-string: string
      sam-cwp-test-url: string
      sam-cwp-username: string
      sam-eap-method: enum
      sam-password: password!sensitive
      sam-private-key: string
      sam-private-key-password: password!sensitive
      sam-report-intv: integer
      sam-security-type: enum
      sam-server-fqdn: string
      sam-server-ip: ipv4-address
      sam-server-type: enum
      sam-ssid: string
      sam-test: enum
      sam-username: string
      short-guard-interval: enum
      transmit-optimize: option-list
      vap-all: enum
      vaps: identifier-list
      wids-profile: string
      zero-wait-dfs: enum
  - name: radio-2
    mode: singleton
    fields:
      80211d: enum
      airtime-fairness: enum
      amsdu: enum
      ap-sniffer-addr: mac-address
      ap-sniffer-bufsize: integer
      ap-sniffer-chan: integer
      ap-sniffer-chan-width: enum
      ap-sniffer-ctl: enum
      ap-sniffer-data: enum
      ap-sniffer-mgmt-beacon: enum
      ap-sniffer-mgmt-other: enum
      ap-sniffer-mgmt-probe: enum
      arrp-profile: string
      auto-power-high: integer
      auto-power-level: enum
      auto-power-low: integer
      auto-power-target: string
      band: option-list
      band-5g-type: enum
      bandwidth-admission-control: enum
      bandwidth-capacity: integer
      beacon-interval: integer
      bss-color: integer
      bss-color-mode: enum
      call-admission-control: enum
      call-capacity: integer
      channel: identifier-list
      channel-bonding: enum
      channel-bonding-ext: enum
      channel-utilization: enum
      coexistence: enum
      darrp: enum
      drma: enum
      drma-sensitivity: enum
      dtim: integer
      frag-threshold: integer
      iperf-protocol: enum
      iperf-server-port: integer
      max-clients: integer
      max-distance: integer
      mimo-mode: enum
      mode: enum
      optional-antenna: enum
      optional-antenna-gain: string
      power-level: integer
      power-mode: enum
      power-value: integer
      powersave-optimize: option-list
      protection-mode: enum
      rts-threshold: integer
      sam-bssid: mac-address
      sam-ca-certificate: string
      sam-captive-portal: enum
      sam-client-certificate: string
      sam-cwp-failure-string: string
      sam-cwp-match-string: string
      sam-cwp-password: password!sensitive
      sam-cwp-success-string: string
      sam-cwp-test-url: string
      sam-cwp-username: string
      sam-eap-method: enum
      sam-password: password!sensitive
      sam-private-key: string
      sam-private-key-password: password!sensitive
      sam-report-intv: integer
      sam-security-type: enum
      sam-server-fqdn: string
      sam-server-ip: ipv4-address
      sam-server-type: enum
      sam-ssid: string
      sam-test: enum
      sam-username: string
      short-guard-interval: enum
      transmit-optimize: option-list
      vap-all: enum
      vaps: identifier-list
      wids-profile: string
      zero-wait-dfs: enum
  - name: radio-3
    mode: singleton
    fields:
      80211d: enum
      airtime-fairness: enum
      amsdu: enum
      ap-sniffer-addr: mac-address
      ap-sniffer-bufsize: integer
      ap-sniffer-chan: integer
      ap-sniffer-chan-width: enum
      ap-sniffer-ctl: enum
      ap-sniffer-data: enum
      ap-sniffer-mgmt-beacon: enum
      ap-sniffer-mgmt-other: enum
      ap-sniffer-mgmt-probe: enum
      arrp-profile: string
      auto-power-high: integer
      auto-power-level: enum
      auto-power-low: integer
      auto-power-target: string
      band: option-list
      band-5g-type: enum
      bandwidth-admission-control: enum
      bandwidth-capacity: integer
      beacon-interval: integer
      bss-color: integer
      bss-color-mode: enum
      call-admission-control: enum
      call-capacity: integer
      channel: identifier-list
      channel-bonding: enum
      channel-bonding-ext: enum
      channel-utilization: enum
      coexistence: enum
      darrp: enum
      drma: enum
      drma-sensitivity: enum
      dtim: integer
      frag-threshold: integer
      iperf-protocol: enum
      iperf-server-port: integer
      max-clients: integer
      max-distance: integer
      mimo-mode: enum
      mode: enum
      optional-antenna: enum
      optional-antenna-gain: string
      power-level: integer
      power-mode: enum
      power-value: integer
      powersave-optimize: option-list
      protection-mode: enum
      rts-threshold: integer
      sam-bssid: mac-address
      sam-ca-certificate: string
      sam-captive-portal: enum
      sam-client-certificate: string
      sam-cwp-failure-string: string
      sam-cwp-match-string: string
      sam-cwp-password: password!sensitive
      sam-cwp-success-string: string
      sam-cwp-test-url: string
      sam-cwp-username: string
      sam-eap-method: enum
      sam-password: password!sensitive
      sam-private-key: string
      sam-private-key-password: password!sensitive
      sam-report-intv: integer
      sam-security-type: enum
      sam-server-fqdn: string
      sam-server-ip: ipv4-address
      sam-server-type: enum
      sam-ssid: string
      sam-test: enum
      sam-username: string
      short-guard-interval: enum
      transmit-optimize: option-list
      vap-all: enum
      vaps: identifier-list
      wids-profile: string
      zero-wait-dfs: enum
  - name: radio-4
    mode: singleton
    fields:
      80211d: enum
      airtime-fairness: enum
      amsdu: enum
      ap-sniffer-addr: mac-address
      ap-sniffer-bufsize: integer
      ap-sniffer-chan: integer
      ap-sniffer-chan-width: enum
      ap-sniffer-ctl: enum
      ap-sniffer-data: enum
      ap-sniffer-mgmt-beacon: enum
      ap-sniffer-mgmt-other: enum
      ap-sniffer-mgmt-probe: enum
      arrp-profile: string
      auto-power-high: integer
      auto-power-level: enum
      auto-power-low: integer
      auto-power-target: string
      band: option-list
      band-5g-type: enum
      bandwidth-admission-control: enum
      bandwidth-capacity: integer
      beacon-interval: integer
      bss-color: integer
      bss-color-mode: enum
      call-admission-control: enum
      call-capacity: integer
      channel: identifier-list
      channel-bonding: enum
      channel-bonding-ext: enum
      channel-utilization: enum
      coexistence: enum
      darrp: enum
      drma: enum
      drma-sensitivity: enum
      dtim: integer
      frag-threshold: integer
      iperf-protocol: enum
      iperf-server-port: integer
      max-clients: integer
      max-distance: integer
      mimo-mode: enum
      mode: enum
      optional-antenna: enum
      optional-antenna-gain: string
      power-level: integer
      power-mode: enum
      power-value: integer
      powersave-optimize: option-list
      protection-mode: enum
      rts-threshold: integer
      sam-bssid: mac-address
      sam-ca-certificate: string
      sam-captive-portal: enum
      sam-client-certificate: string
      sam-cwp-failure-string: string
      sam-cwp-match-string: string
      sam-cwp-password: password!sensitive
      sam-cwp-success-string: string
      sam-cwp-test-url: string
      sam-cwp-username: string
      sam-eap-method: enum
      sam-password: password!sensitive
      sam-private-key: string
      sam-private-key-password: password!sensitive
      sam-report-intv: integer
      sam-security-type: enum
      sam-server-fqdn: string
      sam-server-ip: ipv4-address
      sam-server-type: enum
      sam-ssid: string
      sam-test: enum
      sam-username: string
      short-guard-interval: enum
      transmit-optimize: option-list
      vap-all: enum
      vaps: identifier-list
      wids-profile: string
      zero-wait-dfs: enum
  - name: split-tunneling-acl
    mode: table
    key: <id>
    fields:
      dest-ip: ipv4-classnet
- path: wireless-controller wtp
  mode: table
  key: <wtp-id>
  fields:
    admin: enum
    allowaccess: option-list
    apcfg-profile: string
    ble-major-id: integer
    ble-minor-id: integer
    bonjour-profile: string
    coordinate-latitude: string
    coordinate-longitude: string
    firmware-provision: string
    firmware-provision-latest: enum
    image-download: enum
    index: integer
    ip-fragment-preventing: option-list
    led-state: enum
    location: string
    login-passwd: password!sensitive
    login-passwd-change: enum
    mesh-bridge-enable: enum
    name: string
    override-allowaccess: enum
    override-ip-fragment: enum
    override-lan: enum
    override-led-state: enum
    override-login-passwd-change: enum
    override-split-tunnel: enum
    override-wan-port-mode: enum
    purdue-level: enum
    region: string
    region-x: string
    region-y: string
    split-tunneling-acl-local-ap-subnet: enum
    split-tunneling-acl-path: enum
    tun-mtu-downlink: integer
    tun-mtu-uplink: integer
    uuid: uuid
    wan-port-mode: enum
    wtp-profile: string
  nested:
  - name: lan
    mode: singleton
    fields:
      port-esl-mode: enum
      port-esl-ssid: string
      port-mode: enum
      port-ssid: string
      port1-mode: enum
      port1-ssid: string
      port2-mode: enum
      port2-ssid: string
      port3-mode: enum
      port3-ssid: string
      port4-mode: enum
      port4-ssid: string
      port5-mode: enum
      port5-ssid: string
      port6-mode: enum
      port6-ssid: string
      port7-mode: enum
      port7-ssid: string
      port8-mode: enum
      port8-ssid: string
  - name: radio-1
    mode: singleton
    fields:
      auto-power-high: integer
      auto-power-level: enum
      auto-power-low: integer
      auto-power-target: string
      band: option-list
      channel: identifier-list
      drma-manual-mode: enum
      override-band: enum
      override-channel: enum
      override-txpower: enum
      override-vaps: enum
      power-level: integer
      power-mode: enum
      power-value: integer
      vap-all: enum
      vaps: identifier-list
  - name: radio-2
    mode: singleton
    fields:
      auto-power-high: integer
      auto-power-level: enum
      auto-power-low: integer
      auto-power-target: string
      band: option-list
      channel: identifier-list
      drma-manual-mode: enum
      override-band: enum
      override-channel: enum
      override-txpower: enum
      override-vaps: enum
      power-level: integer
      power-mode: enum
      power-value: integer
      vap-all: enum
      vaps: identifier-list
  - name: radio-3
    mode: singleton
    fields:
      auto-power-high: integer
      auto-power-level: enum
      auto-power-low: integer
      auto-power-target: string
      band: option-list
      channel: identifier-list
      drma-manual-mode: enum
      override-band: enum
      override-channel: enum
      override-txpower: enum
      override-vaps: enum
      power-level: integer
      power-mode: enum
      power-value: integer
      vap-all: enum
      vaps: identifier-list
  - name: radio-4
    mode: singleton
    fields:
      auto-power-high: integer
      auto-power-level: enum
      auto-power-low: integer
      auto-power-target: string
      band: option-list
      channel: identifier-list
      drma-manual-mode: enum
      override-band: enum
      override-channel: enum
      override-txpower: enum
      override-vaps: enum
      power-level: integer
      power-mode: enum
      power-value: integer
      vap-all: enum
      vaps: identifier-list
  - name: split-tunneling-acl
    mode: table
    key: <id>
    fields:
      dest-ip: ipv4-classnet
