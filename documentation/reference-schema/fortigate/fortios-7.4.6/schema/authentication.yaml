domain: authentication
part: 1
parts: 1
sections:
- path: authentication rule
  mode: table
  key: <name>
  fields:
    active-auth-method: string
    cert-auth-cookie: enum
    comments: var-string
    cors-depth: integer
    cors-stateful: enum
    dstaddr: identifier-list
    dstaddr6: identifier-list
    ip-based: enum
    protocol: enum
    srcaddr: identifier-list
    srcaddr6: identifier-list
    srcintf: identifier-list
    sso-auth-method: string
    status: enum
    transaction-based: enum
    web-auth-cookie: enum
    web-portal: enum
- path: authentication scheme
  mode: table
  key: <name>
  fields:
    domain-controller: string
    fsso-agent-for-ntlm: string
    fsso-guest: enum
    kerberos-keytab: string
    method: option-list
    negotiate-ntlm: enum
    require-tfa: enum
    saml-server: string
    saml-timeout: integer
    ssh-ca: string
    user-cert: enum
    user-database: identifier-list
- path: authentication setting
  mode: singleton
  fields:
    active-auth-scheme: string
    auth-https: enum
    captive-portal: string
    captive-portal-ip: ipv4-address-any
    captive-portal-ip6: ipv6-address
    captive-portal-port: integer
    captive-portal-ssl-port: integer
    captive-portal-type: enum
    captive-portal6: string
    cert-auth: enum
    cert-captive-portal: string
    cert-captive-portal-ip: ipv4-address-any
    cert-captive-portal-port: integer
    cookie-max-age: integer
    cookie-refresh-div: integer
    dev-range: identifier-list
    ip-auth-cookie: enum
    persistent-cookie: enum
    sso-auth-scheme: string
    update-time: opaque-user
    user-cert-ca: identifier-list
