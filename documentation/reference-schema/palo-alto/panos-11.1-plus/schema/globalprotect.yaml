phase: 18
family: globalprotect
nodes:
- id: globalprotect-portal
  kind: list
  xpath: '{scope}/global-protect/global-protect-portal'
  scope:
  - vsys
  - template
  - device
  migration_relevance: migration_relevant
  evidence:
    status: repository_observed
    source:
      xpath: '{scope}/global-protect/global-protect-portal'
      repository_path: src/fwmigrate/parsers/palo_alto/globalprotect.py
  entry_key: name
  fields:
    portal_config:
      xml: portal-config
      type: xml-subtree
      custom_handler: true
    client_config:
      xml: client-config
      type: xml-subtree
      custom_handler: true
    satellite_config:
      xml: satellite-config
      type: xml-subtree
      custom_handler: true
    authentication_profile:
      xml: portal-config/authentication-profile
      type: string
      reference:
        kinds:
        - authentication-profile
        cardinality: one
    certificate_profile:
      xml: portal-config/certificate-profile
      type: string
      reference:
        kinds:
        - certificate-profile
        cardinality: one
  custom_handler: true
- id: globalprotect-gateway
  kind: list
  xpath: '{scope}/global-protect/global-protect-gateway'
  scope:
  - vsys
  - template
  - device
  migration_relevance: migration_relevant
  evidence:
    status: repository_observed
    source:
      xpath: '{scope}/global-protect/global-protect-gateway'
      repository_path: src/fwmigrate/parsers/palo_alto/globalprotect.py
  entry_key: name
  fields:
    gateway_config:
      xml: local-address
      type: xml-subtree
      custom_handler: true
    authentication:
      xml: authentication
      type: xml-subtree
      custom_handler: true
    remote_user_tunnel_configs:
      xml: remote-user-tunnel-configs/entry
      type: entry-list
      cardinality: list
      custom_handler: true
    satellite:
      xml: satellite
      type: xml-subtree
      custom_handler: true
  custom_handler: true
- id: globalprotect-network-gateway
  kind: list
  xpath: '{network-scope}/network/global-protect/global-protect-gateway'
  scope:
  - template
  - template-stack
  - device
  - vsys
  migration_relevance: migration_relevant
  evidence:
    status: repository_observed
    source:
      xpath: '{network-scope}/network/global-protect/global-protect-gateway'
      repository_path: src/fwmigrate/parsers/palo_alto/globalprotect.py
  entry_key: name
  fields:
    ip:
      xml: ip
      type: ip
    ipv6:
      xml: ipv6
      type: ip
    interface:
      xml: interface
      type: string
      reference:
        kinds:
        - interface
        cardinality: one
- id: hip-object
  kind: list
  xpath: '{scope}/global-protect/hip-objects'
  scope:
  - shared
  - device-group
  - vsys
  migration_relevance: migration_relevant
  evidence:
    status: repository_observed
    source:
      xpath: '{scope}/global-protect/hip-objects'
      repository_path: src/fwmigrate/parsers/palo_alto/globalprotect.py
  entry_key: name
  fields:
    criteria:
      xml: '*'
      type: xml-subtree
      custom_handler: true
  custom_handler: true
- id: hip-profile
  kind: list
  xpath: '{scope}/global-protect/hip-profiles'
  scope:
  - shared
  - device-group
  - vsys
  migration_relevance: migration_relevant
  evidence:
    status: repository_observed
    source:
      xpath: '{scope}/global-protect/hip-profiles'
      repository_path: src/fwmigrate/parsers/palo_alto/globalprotect.py
  entry_key: name
  fields:
    match:
      xml: match
      type: string
      custom_handler: true
  custom_handler: true
