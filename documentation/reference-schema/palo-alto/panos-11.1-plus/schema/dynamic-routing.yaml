phase: 9
family: dynamic-routing
nodes:
- id: bgp
  kind: singleton
  xpath: '{network-scope}/network/virtual-router/entry/protocol/bgp'
  scope:
  - template
  - template-stack
  - device
  - vsys
  migration_relevance: migration_relevant
  evidence:
    status: repository_observed
    source:
      xpath: '{network-scope}/network/virtual-router/entry/protocol/bgp'
      repository_path: src/fwmigrate/parsers/palo_alto/dynamic_routing.py
  fields:
    enable:
      xml: enable
      type: boolean
    router_id:
      xml: router-id
      type: ip
    local_as:
      xml: local-as
      type: string
    peer_groups:
      xml: peer-group/entry
      type: entry-list
      cardinality: list
      custom_handler: true
    routing_options:
      xml: routing-options
      type: xml-subtree
      migration_relevance: source_evidence_only
    redist_rules:
      xml: redist-rules/entry
      type: entry-list
      cardinality: list
      custom_handler: true
    policy_import:
      xml: policy/import/rules/entry
      type: entry-list
      cardinality: list
      custom_handler: true
    policy_export:
      xml: policy/export/rules/entry
      type: entry-list
      cardinality: list
      custom_handler: true
  custom_handler: true
- id: ospf
  kind: singleton
  xpath: '{network-scope}/network/virtual-router/entry/protocol/ospf'
  scope:
  - template
  - template-stack
  - device
  - vsys
  migration_relevance: migration_relevant
  evidence:
    status: repository_observed
    source:
      xpath: '{network-scope}/network/virtual-router/entry/protocol/ospf'
      repository_path: src/fwmigrate/parsers/palo_alto/dynamic_routing.py
  fields:
    enable:
      xml: enable
      type: boolean
    router_id:
      xml: router-id
      type: ip
    areas:
      xml: area/entry
      type: entry-list
      cardinality: list
      custom_handler: true
    export_rules:
      xml: export-rules/entry
      type: entry-list
      cardinality: list
      custom_handler: true
    auth_profiles:
      xml: auth-profile/entry
      type: entry-list
      cardinality: list
      migration_relevance: source_evidence_only
  custom_handler: true
- id: ospfv3
  kind: singleton
  xpath: '{network-scope}/network/virtual-router/entry/protocol/ospfv3'
  scope:
  - template
  - template-stack
  - device
  - vsys
  migration_relevance: migration_relevant
  evidence:
    status: repository_observed
    source:
      xpath: '{network-scope}/network/virtual-router/entry/protocol/ospfv3'
      repository_path: src/fwmigrate/parsers/palo_alto/dynamic_routing.py
  fields:
    enable:
      xml: enable
      type: boolean
    router_id:
      xml: router-id
      type: ip
    areas:
      xml: area/entry
      type: entry-list
      cardinality: list
      custom_handler: true
    export_rules:
      xml: export-rules/entry
      type: entry-list
      cardinality: list
      custom_handler: true
  custom_handler: true
- id: redistribution-profile
  kind: list
  xpath: '{network-scope}/network/virtual-router/entry/protocol/redist-profile'
  scope:
  - template
  - template-stack
  - device
  - vsys
  migration_relevance: migration_relevant
  evidence:
    status: repository_observed
    source:
      xpath: '{network-scope}/network/virtual-router/entry/protocol/redist-profile'
      repository_path: src/fwmigrate/parsers/palo_alto/dynamic_routing.py
  entry_key: name
  fields:
    priority:
      xml: priority
      type: integer
    action:
      xml: action
      type: enum
    filter:
      xml: filter
      type: xml-subtree
      custom_handler: true
  custom_handler: true
