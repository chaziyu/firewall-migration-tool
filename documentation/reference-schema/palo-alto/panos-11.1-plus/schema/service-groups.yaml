phase: 4
family: service-groups
nodes:
- id: service-group
  kind: list
  xpath: '{scope}/service-group'
  scope: [shared, device-group, vsys]
  migration_relevance: migration_relevant
  evidence:
    status: repository_observed
    source:
      xpath: '{scope}/service-group/entry'
      repository_path: src/fwmigrate/parsers/palo_alto/transformer.py
  entry_key: name
  fields:
    members:
      xml: members/member
      type: member-list
      cardinality: list
      reference:
        kinds: [service, service-group]
        cardinality: many
        scope_resolution: current->ancestors->shared
    tags:
      xml: tag/member
      type: member-list
      cardinality: list
      reference:
        kinds: [tag]
        cardinality: many
        scope_resolution: current->ancestors->shared
